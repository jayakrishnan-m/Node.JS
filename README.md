# Apptega Public Api

## Installation

>1.Run the following commands
```bash
$ npm install serverless --global
$ cd path/to/project/directory
$ npm install
```
>2.Rename **.env.json.example** file into **.env.json**

>3.Setup your database credentials in **.env.json** file

## Usage
Run the following command to test the project in local using **serverless-offline** plugin
```bash
$ serverless offline
```

